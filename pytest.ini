[pytest]
# I test coprono due componenti che vivono in cartelle separate e non si
# importano a vicenda: l'API e l'agent privilegiato.
pythonpath = backend agent
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning:app.*
